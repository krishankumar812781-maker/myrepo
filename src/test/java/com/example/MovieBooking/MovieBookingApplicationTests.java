package com.example.MovieBooking;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class MovieBookingApplicationTests {

    @Test
    void contextLoads() {
        // This test is empty on purpose.
        // It passes if the Spring application context loads successfully.
        // It fails if there is an error during startup.
    }
}